#!/bin/bash
## Description: Run the Vite dev server on port 5173 for local frontend hot reload.
## Usage: vite

set -euo pipefail

if [ ! -d node_modules ]; then
  echo "Installing frontend dependencies..."
  npm install
fi

exec npm run dev -- --host 0.0.0.0 --port "${VITE_PORT:-5173}"
